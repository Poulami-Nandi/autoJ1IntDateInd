# --- Portal URLs (use the actual ones for India J-1 scheduling) ---
START_URL="https://<the-portal-start-url>"               # login landing (e.g., CGI Federal / USTravelDocs start)
SCHEDULE_URL="https://<the-schedule-page-url>"           # where the calendar/next available shows after login

# --- Locations you want to check (identifiers or page paths/anchors you’ll click) ---
CONSULATE_KEYS="mumbai,delhi,hyderabad,chennai,kolkata"

# CSS/XPath selectors to find availability text or date element on the schedule page
# You may need to tweak these after inspecting the page (F12) once logged in.
AVAIL_SELECTOR=".next-available-date, .calendar-note, .availability, .slot-info"

# --- Notification (Telegram) ---
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""

# --- Notification (Email via SMTP) ---
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM=""
EMAIL_TO="nandi.poulami91@gmail.com"

# --- Behavior ---
CHECK_INTERVAL_SECONDS="600"        # if you run watcher.py as a daemon (optional)
RANDOM_SLEEP_JITTER="30"            # adds randomness to avoid rate patterns
EARLIEST_TARGET_DATE="2025-10-15"   # alert only if earlier than this (optional, ISO YYYY-MM-DD)
